#função que abra um arquivo .nes para leitura
#(ok) a) verificar se o arquivi é valido ou seja se está no formato especificado para o nes
#(ok) b) caso negativo informar incompatibilidade
#(ok) c) criar ED que represente o cabeçalho do arquivo e cada um dos campos de dados e programa
#(ok) d) separar cada segmento de codigo e salva-los na ED
# e) ajustar o pc para apontar para o 1º bite valido de codigo
# f) criar uma função que retorna o byte apontado pelo PC (mnemonico - instrução)
# g) criar uma função que com base no byte acima decodifica a intrução (binario -> assembly)
# h) utilizar mascaras de bits para decidir com base no mnemonico a quantidade de argumentos da intrução
# i) criar uma função que recebe como parametro o numero de bytes (1 ou 2) a partir do PC que devem ser retornados
# j) toda função que manipula o PC deve atualiza-lo
#(ok) k) criar um dicionario de correspondencia entre codigo de maquina e assembly
# l)

'''
(ok)-> Estudar o pygame
(ok)-> criar um 'menu' para o emulador
-> criar tela de transcript (deBug)
'''